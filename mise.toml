# Pin CLI/tool versions for local development (see README — Local development).
# Install: https://mise.jdx.dev/getting-started.html
# Align `just` with docker/Dockerfile (just.systems install --tag 1.42.4).

[tools]
python = "3.12"
uv = "0.11.12"
just = "1.42.4"
